# Molecule managed
---
file:
  /usr/bin/vagrant:
    exists: true
    owner: root
    group: root

command:
  help:
    exit-status: 0
    exec: "vagrant --help 2>&1"
    stdout:
      - "/vagrant/"
    stderr: []
    timeout: 10000      # in milliseconds
    skip: false
