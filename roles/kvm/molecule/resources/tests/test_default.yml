# Molecule managed
---
file:
  /usr/bin/virsh:
    exists: true
    owner: root
    group: root

command:
  help:
    exit-status: 0
    exec: "virsh help"
    stdout:
      - "/virsh/"
    stderr: []
    timeout: 10000      # in milliseconds
    skip: false
