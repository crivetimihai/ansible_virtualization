# Molecule managed
---
file:
  /usr/bin/vagrant-box:
    exists: true
    owner: root
    group: root

command:
  help:
    exit-status: 0
    exec: "vagrant-box --help"
    stdout:
      - "/vagrant-box/"
    stderr: []
    timeout: 10000      # in milliseconds
    skip: false
