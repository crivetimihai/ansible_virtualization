# Molecule managed
---
file:
  /usr/local/bin/packer:
    exists: true
    owner: root
    group: root

command:
  help:
    exit-status: 0
    exec: "packer --help"
    stdout:
      - "/packer/"
    stderr: []
    timeout: 10000      # in milliseconds
    skip: false
