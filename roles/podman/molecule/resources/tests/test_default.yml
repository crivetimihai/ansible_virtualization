# Molecule managed
---
file:
  /usr/bin/podman:
    exists: true
    owner: root
    group: root

command:
  help:
    exit-status: 0
    exec: "podman --help"
    stdout:
      - "/podman/"
    stderr: []
    timeout: 10000      # in milliseconds
    skip: false
