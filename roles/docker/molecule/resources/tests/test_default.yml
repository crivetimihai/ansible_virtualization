# Molecule managed
---
file:
  /usr/bin/docker:
    exists: true
    owner: root
    group: root

command:
  help:
    exit-status: 0
    exec: "docker help"
    stdout:
      - "/docker/"
    stderr: []
    timeout: 100000      # in milliseconds
    skip: false
